/** Debounced live search used by the global header. */
(function(){'use strict';document.addEventListener('DOMContentLoaded',()=>{const input=document.querySelector('[data-global-search]'),results=document.querySelector('[data-search-results]');if(!input||!results)return;let timer;input.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>{const q=input.value.trim().toLowerCase();if(q.length<2){results.innerHTML='';return;}const matches=StoreDB.getProducts().filter(p=>[p.name,p.category,p.description,...(p.tags||[])].join(' ').toLowerCase().includes(q)).slice(0,6);results.innerHTML=matches.length?matches.map(p=>`<a class="search-result" href="product.html?id=${p.id}"><img src="${p.images[0]}" alt=""><span><strong>${p.name}</strong><br><small>${p.category} · ${StoreUI.money(p.price)}</small></span></a>`).join(''):'<p>No matching pieces found.</p>';},180);});});})();

